#function detect_cyclone(array,threshold)
#    surf_pres_filtered = smoothfilter(array,threshold);
#    peaks              = findlocalmaxima(surf_pres_filtered);
#    return  seeded_region_growing(array, seeds)
#end